$("#comments_area").html(
  "<%= j(render "comments/index", { comments: @blog.comments.order(created_at: :desc), blog: @blog }) %>"
  );
$("textarea").val("");
$("#notice").html("<%= j flash[:notice] %>");