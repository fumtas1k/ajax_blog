<div id="follow_form_<%= user.id %>">
  <% unless current_user == user %>
    <% unless current_user.following?(user) %>
      <%= link_to "フォロー", relationships_path(relationship: {followed_id: user.id}), method: :post, remote: true, class: "btn btn-primary" %>
    <% else %>
      <%= link_to "フォロー中", relationship_path(current_user.following?(user)), method: :delete, remote: true, class: "btn btn-outline-primary" %>
    <% end %>
    <!-- <% unless current_user.following?(user) %>
      <%=form_with(model: current_user.active_relationships.build(followed_id: user.id)) do |f| %>
        <%= f.hidden_field :followed_id %>
        <%= f.submit "フォロー", class: "btn btn-primary" %>
      <% end %>
    <% else %>
      <%= form_with(model: current_user.following?(user), method: :delete) do |f| %>
        <%= f.submit "フォロー中", class: "btn btn-outline-primary" %>
      <% end %>
    <% end %> -->
  <% end %>
</div>